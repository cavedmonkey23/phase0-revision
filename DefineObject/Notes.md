```JavaScript
//Define an object
const person = {
    name: 'John',
    age : 30,
    occupation:"Web Developer"
};
```
note that:
- the key is always a string.
- values can be of any type (string , number etc).
